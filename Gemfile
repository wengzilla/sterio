source 'https://rubygems.org'
ruby '2.0.0'

gem 'rails', '3.2.13'

group :test, :development do
  gem 'sqlite3'
  gem 'taps'
end

group :production do
  gem 'pg'
  gem 'therubyracer'
end

group :assets do
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

# push
gem 'pubnub'

# styling
gem 'sass'
gem 'sass-rails',   '~> 3.2.3'
gem 'compass-rails'
gem 'jquery-ui-rails'
gem 'jquery-rails'
gem 'bourbon'
gem 'font-awesome-rails'

# authentication
gem 'sorcery', '~> 0.7.13'
gem 'valid_email'

# youtube searching
gem 'youtube_it'

# helps with playlist ordering and reordering
gem 'acts_as_list'
gem 'awesome_print'

# Search optimization help
gem 'spiderable'

# tokens
gem 'uniquify'

# storing the top itunes songs
gem 'httparty'
gem 'redis'

gem 'debugger'