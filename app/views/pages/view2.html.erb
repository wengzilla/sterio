<input type="text" ng-model="newEntry.name" />
<button ng-click="addEntry()">Add Entry</button>

<ul>
  <li ng-repeat="entry in entries">{{entry.name}}</li>
</ul>

<button ng-click="draw()">Draw a winner!</button>